import React from 'react'

const Email = ({ onNext }) => {
    return (
      <div>
        {/* Your email form content */}
        <button onClick={onNext}>Next</button>
      </div>
    );
  };
export default Email